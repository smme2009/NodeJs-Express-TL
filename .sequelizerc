const Path = require('path');

const basePath = __dirname;
const databasePath = Path.join(basePath, "src/database");

module.exports = {
    "config": Path.resolve(basePath, "database.config.js"),
    "models-path": Path.resolve(databasePath, "model"),
    "seeders-path": Path.resolve(databasePath, "seeder"),
    "migrations-path": Path.resolve(databasePath, "migration"),
};
